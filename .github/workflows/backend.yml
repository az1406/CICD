name: Backend CI/CD

on:
  push:
    branches:
      - main
      - deploy/production

jobs:
  build-deliver-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/secret-notes-backend:latest

      #- name: Deploy to EC2
       # uses: appleboy/ssh-action@v0.1.7
        #with:
         # host: ${{ secrets.EC2_HOST }}
          #username: ubuntu
          #key: ${{ secrets.EC2_SSH_KEY }}
          #script: |
           # cd ~/your-app-folder
            #docker pull ${{ secrets.DOCKERHUB_USERNAME }}/secret-notes-backend:latest
            #docker stop secret-notes-backend || true
            #docker rm secret-notes-backend || true
            #docker run -d --name secret-notes-backend -p 3000:3000 ${{ secrets.DOCKERHUB_USERNAME }}/secret-notes-backend:latest
